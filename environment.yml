# $ micromamba create -f environment.yml
# $ micromamba activate seldonenv
#

name: mfpt_env
channels:
  - conda-forge
# requirements:
#   build:
#     - {{ compiler('c') }}
#     - {{ compiler('c++') }}
dependencies:
  - meson
  - cmake
  - ninja 
  - cpp-argparse
  - clang-format
  - tomlplusplus
  - xtensor